 #include <print>

/*
struct Args {
	enum class Mode : uint8_t { Encrypt, Decrypt, Help };
	Mode mode = Mode::Help;
	bool error = false;
	std::string error_msg;
};

void PrintHelp(const char* program_name) {
	print("Використання:");
	print("  -h, --help    Показати цю довідку.");
}

static Args parse_args(int argc, char* argv[]) noexcept {
    Args a;
    for (int i = 1; i < argc; ++i) {
        const std::string arg = argv[i];

        if (arg == "-h" || arg == "--help") a.mode = Args::Mode::Help;
        else if (arg == "-e" || arg == "--primality")   a.mode = Args::Mode::Encrypt;
        else if (arg == "-d" || arg == "--benchmark")   a.mode = Args::Mode::Decrypt;

    return a;
}


int main() {
    

	switch (args.mode) {
        case Args::Mode::Help: {
            PrintHelp(argv[0]);
            return 0;
        }
        case Args::Mode::Cipher: {
            print("Режим шифрування.");
            return rabbit;
        }
        case Args::Mode::Decipher: {
            print("Режим дешифрування.");
            return rabbit;
		}
	
	}

	return 0;
}*/

int main() {

    return 0;
}